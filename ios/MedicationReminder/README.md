# 用药提醒 iOS

这是独立的 SwiftUI 原生版本，不依赖现有 Web 或微信小程序构建。

## 打开方式

1. 安装完整 Xcode。
2. 打开 `MedicationReminder.xcodeproj`。
3. 选择 iPhone 模拟器或真机运行。

当前机器只安装了 Command Line Tools，没有完整 Xcode，所以暂时不能用 `xcodebuild` 验证 iOS 构建。
